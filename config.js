(function (global) {
    global.__config__ = {
        // API_PATH:'/portal',    // 线上请求地址(直连)
        API_PATH:'/portal/compliance',    // 线上请求地址(转)
        // API_PATH:'/api',    // 请求地址
        // API_PATH:'/test',    // 本地请求地址
        // PORTAL_PATH:'/portal',    // 本地请求地址
    }
})(window)
